<view class="container">
  <tui-tabs tabs="{{tabs3}}" isFixed="{{true}}" currentTab="{{currentTab}}" bind:change="change" bottom="50%" bgColor="{{bgColor}}" color="#fff" selectedColor="#EB0909" sliderWidth="{{146}}" sliderHeight="{{52}}" sliderBgColor="#fff" unlined="{{true}}"></tui-tabs>
  <view class="header">
    <view class="title">tabs</view>
    <view class="sub-title">tabs标签页，支持自定义设置，具体查看文档。暂不支持超出一屏，滚动标签页
      <text class="tui-primary" bindtap="goNavBar">参考这里</text>。
    </view>
  </view>
  <tui-tabs tabs="{{navbar}}" currentTab="{{currentTab>1?0:currentTab}}" bind:change="change" itemWidth="50%"></tui-tabs>
  <view class="tui-mtop">
    <tui-tabs tabs="{{tabs}}" currentTab="{{currentTab>2?0:currentTab}}" bind:change="change" itemWidth="33.3333%"></tui-tabs>
  </view>
  <view class="tui-mtop">
    <tui-tabs tabs="{{tabs2}}" currentTab="{{currentTab}}" selectedColor="#EB0909" sliderBgColor="#EB0909" bind:change="change"></tui-tabs>
  </view>
  <view class="tui-mtop">
    <tui-tabs tabs="{{tabs2}}" height="{{88}}" currentTab="{{currentTab}}" sliderWidth="{{150}}" sliderHeight="{{60}}" bottom="50%" color="#888" selectedColor="#11CDFC" bold="{{true}}" sliderBgColor="#E5FAFF" bind:change="change"></tui-tabs>
  </view>
</view>