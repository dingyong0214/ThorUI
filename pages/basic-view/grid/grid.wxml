<view class="container">
  <view class="tui-title">
    Grid-2列
  </view>
  <tui-grid>
    <block wx:for="{{dataList}}" wx:key="{{index}}">
      <tui-grid-item cell="2" bind:click="detail">
        <view class="tui-grid-icon">
          <tui-icon name="{{item.name}}" size="{{item.size}}" color="{{item.color}}"></tui-icon>
        </view>
        <text class="tui-grid-label">{{item.name}}</text>
      </tui-grid-item>
    </block>
  </tui-grid>

  <view class="tui-title">
    Grid-3列
  </view>
  <tui-grid>
    <block wx:for="{{dataList}}" wx:key="{{index}}">
      <tui-grid-item cell="3" bind:click="detail">
        <view class="tui-grid-icon">
          <tui-icon name="{{item.name}}" size="{{item.size}}" color="{{item.color}}"></tui-icon>
        </view>
        <text class="tui-grid-label">{{item.name}}</text>
      </tui-grid-item>
    </block>
  </tui-grid>

  <view class="tui-title">
    Grid-4列
  </view>
  <tui-grid>
    <block wx:for="{{dataList}}" wx:key="{{index}}">
      <tui-grid-item cell="4" tui-griditem-class="tui-grid-four" bind:click="detail">
        <view class="tui-grid-icon">
          <tui-icon name="{{item.name}}" size="{{item.size}}" color="{{item.color}}"></tui-icon>
        </view>
        <text class="tui-grid-label">{{item.name}}</text>
      </tui-grid-item>
    </block>
  </tui-grid>

  <view class="tui-title">
    Grid-5列
  </view>
  <tui-grid>
    <block wx:for="{{iconList}}" wx:key="{{index}}">
      <tui-grid-item cell="5" tui-griditem-class="tui-grid-five"  bgcolor="#fcebef" bind:click="detail">
        <view class="tui-grid-icon">
          <tui-icon name="{{item.name}}" size="{{item.size}}" color="#8a5966"></tui-icon>
        </view>
        <text class="tui-grid-label tui-grid-5">{{item.name}}</text>
      </tui-grid-item>
    </block>
  </tui-grid>
 <view class="tui-title">
    Grid-5列
  </view>
  <tui-grid>
    <block wx:for="{{iconList}}" wx:key="{{index}}">
      <tui-grid-item cell="5" tui-griditem-class="tui-grid-five" bind:click="detail">
        <view class="tui-grid-icon">
          <tui-icon name="{{item.name}}" size="{{item.size}}" color="#8a5966"></tui-icon>
        </view>
        <text class="tui-grid-label tui-grid-5">{{item.name}}</text>
      </tui-grid-item>
    </block>
  </tui-grid>
</view>