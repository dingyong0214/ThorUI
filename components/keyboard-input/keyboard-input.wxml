<view class="tui-keyboard-input tui-pwd-box" style="background:{{bgcolor}}">
  <view class="tui-inner-box">
    <view class="tui-input {{inputvalue.length===4?'tui-margin-right':''}}" style="font-size:{{size}}rpx;color:{{color}};width:{{inputvalue.length===4?90:70}}rpx" wx:for="{{inputvalue}}" wx:key="{{index}}">{{item}}</view>
  </view>
</view>